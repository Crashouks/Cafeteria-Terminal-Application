# ЛР1. Реалізація консольного застосунку мовою Java

**Тема:** Облік замовлень у кав'ярні "Ранкова Хвиля"

## Предметна область та сутність

Ключова сутність програми — **Замовлення** (`Order`), яке відображає один чек клієнта кав'ярні.

### Характеристики сутності `Order`, що вводяться користувачем

| Поле              | Тип      | Опис                                  |
|-------------------|----------|----------------------------------------|
| `customerName`    | `String` | Ім'я клієнта                          |
| `drinkName`       | `String` | Назва напою                           |
| `volumeMl`        | `int`    | Об'єм порції, мл                      |
| `pricePerUnit`    | `double` | Ціна за одну порцію, грн              |
| `quantity`        | `int`    | Кількість порцій                      |
| `discountPercent` | `double` | Знижка клієнта, %                     |

Це 6 введених значень трьох різних типів (`String`, `int`, `double`) — більше за мінімальні 4–5, вимогані завданням.

Додатково вводиться дата зміни (`String`) і план виручки на зміну (`double`), які використовуються для підсумкового обліку.

### Обчислювальні операції

1. **Визначення розміру порції** (`sizeLabel`) за об'ємом: Малий / Середній (M) / Великий (L) — розгалуження через вкладений тернарний оператор.
2. **Бонус за лояльність**: якщо кількість порцій ≥ 3, до знижки клієнта додається +5%.
3. **Сумарна знижка** (`totalDiscount`) — знижка клієнта + бонус, обмежена зверху 100% (`Math.min`).
4. **Сума без знижки** (`subtotal = pricePerUnit * quantity`).
5. **Сума знижки у грошовому виразі** (`discountAmount = subtotal * totalDiscount / 100`).
6. **Сума до сплати** (`total = subtotal - discountAmount`).
7. **Підсумок зміни**: загальна виручка, середній чек, найбільший чек, відхилення від плану — розраховуються після обробки обох замовлень.

Усі дробові результати виводяться через `System.out.printf` з форматом `%.2f` (2 знаки після коми), як вимагає завдання.

## Приклад запуску

```
=== Кав'ярня "Ранкова Хвиля" — приймання та облік замовлень ===
Дата зміни (напр., 10.09.2026): 10.09.2026
Плановий обсяг виручки за зміну, грн: 1000

--- Замовлення №1 ---
Ім'я клієнта: Олена
Назва напою: Лате
Об'єм порції, мл: 350
Ціна за порцію, грн: 65
Кількість порцій: 2
Знижка клієнта, %: 0

--- Замовлення №2 ---
Ім'я клієнта: Маркіян
Назва напою: Американо
Об'єм порції, мл: 450
Ціна за порцію, грн: 55.5
Кількість порцій: 4
Знижка клієнта, %: 10

================ ЧЕК №2 ================
Клієнт:            Маркіян
Напій:             Американо (Великий (L), 450 мл)
Сума без знижки:   222.00 грн
Знижка:            15.00 % (33.30 грн)
До сплати:         188.70 грн
==========================================

============ ОБЛІК ЗМІНИ (10.09.2026) ============
Кількість замовлень:      2
Загальна виручка:         318.70 грн
Середній чек:             159.35 грн
Найбільший чек:           188.70 грн (клієнт: Маркіян)
План на зміну:            1000.00 грн
Відхилення від плану:     -681.30 грн
Статус плану:             НЕ ВИКОНАНО
=======================================================
```

## Інструкція запуску

Потрібен встановлений JDK (перевірено на JDK 21).

```bash
javac Main.java
java Main
```

Якщо кирилиця виводиться некоректно (типово на Windows у cmd), запускайте з явним кодуванням:

```bash
javac -encoding UTF-8 Main.java
java -Dfile.encoding=UTF-8 -Dstdout.encoding=UTF-8 Main
```

Програма запитує дані для двох замовлень поспіль, друкує чек для кожного, а в кінці — зведений облік зміни.